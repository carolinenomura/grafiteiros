# Recomendador de Filmes com Algoritmo
## 🚀 Setup

Siga estas instruções para configurar e executar o projeto em sua máquina local.

### Pré-requisitos

Você precisará ter o [Node.js](https://nodejs.org/en/) (versão 18 ou superior) e o [NPM](https://www.npmjs.com/) instalado.

### Instalação e Execução

1.  Clone o repositório:
    ```bash
    git clone [link-do-repositório]
    ```

2.  Para configurar o frontend, navegue até o diretório `frontend`:
    ```bash
    cd frontend
    ```

3.  Instale as dependências:
    ```bash
    npm install
    ```

4.  Execute com o comando:
    ```bash
    npm run dev
    ```

5.  Abra [http://localhost:5173](http://localhost:5173) (ou a porta indicada no terminal) no seu navegador para ver o projeto.
